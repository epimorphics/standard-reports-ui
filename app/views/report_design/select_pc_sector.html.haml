= render partial: "help"
.select-pc-sector

  %h1.heading-medium
    = render partial: "progress_indicator", locals: {workflow: @workflow}
    Select a postcode sector

  %p
    Choose which postcode sector the report should cover. A postcode sector is
    the first part of a UK postcode, up to and including the first digit after the space.
    For example &quot;B17 0&quot; or &quot;TA9 3&quot;

  = workflow_step_form( @workflow )
