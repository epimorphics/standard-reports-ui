= content_for(:title, "#{@workflow.step_progress_summary} &ndash; Report Design".html_safe)

= render partial: "help"
.select-district

  %h1.heading-medium
    = render partial: "progress_indicator", locals: {workflow: @workflow}
    Select a district

  %p
    Enter the name of the district (i.e. local authority) you wish the report to cover:

  = workflow_step_form( @workflow )

  :javascript
    $( function() {
      $("[name=area]").autocomplete( {
        source: window.lr.data.district
      } )
    } );
