#!/bin/bash
HOST=${LR_PRES_HOST:-LR-Pres-1-b}

while true; do
  ssh_running=`ps -fC ssh | grep $HOST`
  if [[ "${ssh_running:-null}" = null ]];then
    echo "Starting ssh tunnel"
    ssh -f $HOST -L 8080:localhost:8080 -N
  fi
  sleep 5
done

